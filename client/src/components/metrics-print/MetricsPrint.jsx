import React, { useState, useRef, useEffect, forwardRef } from "react";
import { useReactToPrint } from "react-to-print";

const MetricsPrint = ({ props, ref }) => {
  return <div>MetricsPrint</div>;
};

export default MetricsPrint;
